export const ROWS = 25;
export const COLS = 50;

export const DEFAULT_START_NODE = { row: 12, col: 10 };
export const DEFAULT_END_NODE = { row: 12, col: 40 };

export const SPEEDS = {
  SLOW: 50,
  MEDIUM: 20,
  FAST: 5,
  INSTANT: 0
};
